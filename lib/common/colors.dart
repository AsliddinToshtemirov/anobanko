import 'package:flutter/material.dart';

const mainBg = (Colors.white);

///for button
const primaryColor = Color(0xff4263EB);

const accentColor = Color(0xffffffff);
const secondButtonColors = Color(0xffF5F7FE);
